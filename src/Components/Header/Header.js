import React from 'react';
import Menubar from '../Shared/MenuBar/Menubar';
import './Header.css'

const Header = () => {
    return (
        <div>
<Menubar/>
        </div>
    );
};

export default Header;